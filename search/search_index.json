{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Welcome to Xians.ai Documentation","text":"<p>Welcome to the official documentation for Xians.ai. Here you'll find everything you need to get started with our platform and make the most of our services.</p>"},{"location":"#quick-navigation","title":"Quick Navigation","text":"<ul> <li>Getting Started - Set up your environment and create your first flow</li> </ul>"},{"location":"#getting-help","title":"Getting Help","text":"<ul> <li>Check our documentation</li> <li>Visit our GitHub repository</li> <li>Contact our support team at support@xians.ai</li> </ul>"},{"location":"#latest-updates","title":"Latest Updates","text":"<p>Stay tuned for the latest updates and features. Follow our changelog for detailed release information.</p>"},{"location":"getting-started/1-setting-up/","title":"Setting Up a Flow Project","text":""},{"location":"getting-started/1-setting-up/#prerequisites","title":"Prerequisites","text":"<ul> <li>.NET 9 SDK</li> </ul>"},{"location":"getting-started/1-setting-up/#creating-a-dotnet-project","title":"Creating a DotNet Project","text":"<p>Workflow Projects are Executable DotNet Projects that can be run locally and deployed to any server environment.</p> <p>To create a new DotNet Console Project, you can use the following command:</p> <pre><code>dotnet new console -n MyNewXiansAiFlow\ncd MyNewXiansAiFlow\n</code></pre>"},{"location":"getting-started/1-setting-up/#adding-the-xiansai-sdk","title":"Adding the Xians.ai SDK","text":"<p>To add the Xians.ai SDK to your project, you can use the following command:</p> <pre><code>dotnet add package XiansAi.Lib\n</code></pre> <p>Expert Tip</p> <p>If you are using locally cloned XiansAi.Lib repository, add a Project Reference instead of the Nuget Package. E.g., <pre><code>&lt;ItemGroup&gt;\n    &lt;ProjectReference Include=\"..\\XiansAi.Lib\\XiansAi.Lib.csproj\" /&gt;\n&lt;/ItemGroup&gt;\n</code></pre></p>"},{"location":"getting-started/1-setting-up/#create-a-new-flow","title":"Create a new flow","text":"<p>You will create a new flow and the necessary activities that will be used in the flow. These flows will run your local machine while development, and you may deploy to any server environment.</p> <p>XiansAi.Lib is the library that connects your flow with the XiansAI platform. There are 2 parts in the XiansAi platform:</p> <ol> <li>App Server: This is the server that allows you to manage your flows and instructions. It provides visibility into the flow execution and allows you to manage your flows and instructions.</li> <li>Flow Server: This is the server that will run your flow. It is the server that will be deployed to the XiansAI platform.</li> </ol> <p>Next we will download the necessary certificates to configure the Flow Runner.</p>"},{"location":"getting-started/1-setting-up/#download-necessary-certificates","title":"Download Necessary Certificates","text":"<p>To create a new flow, we need a create a <code>FlowRunnerService</code> instance and provide it with the 'flow' and 'activity' classes.</p> <p>To configure the <code>FlowRunnerService</code> with XiansAi platform, add the following code to your <code>Program.cs</code>.</p> <p><code>Program.cs &gt;</code></p> <pre><code>using XiansAi.Flow;\n\nEnvironment.SetEnvironmentVariable(\"APP_SERVER_URL\", \"https://xiansai-server.azurewebsites.net\");\nEnvironment.SetEnvironmentVariable(\"APP_SERVER_CERT_PATH\", \"./certs/AppServerCert-1736843478734.pfx\");\nEnvironment.SetEnvironmentVariable(\"APP_SERVER_CERT_PWD\", \"my-password\");\nEnvironment.SetEnvironmentVariable(\"FLOW_SERVER_URL\", \"tenant-99x.ozqzb.tmprl.cloud:7233\");\nEnvironment.SetEnvironmentVariable(\"FLOW_SERVER_NAMESPACE\", \"tenant-99x.ozqzb\");\nEnvironment.SetEnvironmentVariable(\"FLOW_SERVER_CERT_PATH\", \"./certs/FlowServerCert-1736843586061.crt\");\nEnvironment.SetEnvironmentVariable(\"FLOW_SERVER_PRIVATE_KEY_PATH\", \"./certs/FlowServerPrivateKey-1736843587832.key\");\n\nvar flowRunner = new FlowRunnerService(config);\n\n// Register the flow (see the next section for more details)\n</code></pre> <p>You can find the settings required to run the <code>FlowRunnerService</code> on the XiansAi portal's <code>Settings</code> section.</p> <p></p> <p>Example <code>Config</code> object:</p> <pre><code>var config = new PlatformConfig {\n    AppServerUrl = \"https://xiansai-server.azurewebsites.net \", \n    AppServerCertPath = \"./certs/AppServerCert-1736843478734.pfx\", \n    AppServerCertPwd = \"my-password\", \n    FlowServerUrl = \"tenant-99x.ozqzb.tmprl.cloud:7233\", \n    FlowServerNamespace = \"tenant-99x.ozqzb\", \n    FlowServerCertPath = \"./certs/FlowServerCert-1736843586061.crt\", \n    FlowServerPrivateKeyPath = \"FlowServerPrivateKey-1736843587832.key\" \n};\n</code></pre> <p>Use the values from the XiansAI portal to configure the <code>Config</code> object. Set the local path to the certificate files you downloaded from the XiansAI portal.</p> <p>Expert Tip</p> <p>You can use DotNetEnv package to load the environment variables from the <code>.env</code> file without hardcoding them in your code.</p>"},{"location":"getting-started/1-setting-up/#validate-the-configuration","title":"Validate the configuration","text":"<p>To validate the configuration, lets temporarily call <code>TestMe()</code> method from the <code>FlowRunnerService</code> instance.</p> <pre><code>...\nvar flowRunner = new FlowRunnerService(config);\nflowRunner.TestMe(); // temp method to validate the configuration\n</code></pre> <p>Now run the following command:</p> <pre><code>dotnet run\n</code></pre> <p>If you do not get errors, your configuration is correct. You can now remove the test call to <code>TestMe()</code> method in your code.</p>"},{"location":"getting-started/1-setting-up/#next-steps","title":"Next Steps","text":"<p>Now that you have set up your environment, you can proceed to create your first flow.</p>"},{"location":"getting-started/2-first-flow/","title":"Your First Flow","text":""},{"location":"getting-started/2-first-flow/#creating-a-flow","title":"Creating a Flow","text":"<p>You can create a new flow by creating a new class that inherits from <code>XiansAi.Flow.FlowBase</code>.</p> <p>Tip</p> <p>We are creating a simple flow. We will learn about more complex and meaningful flows that users <code>Agents</code> and <code>Instructions</code> in the next sections.</p> <p><code>SimpleFlow.cs &gt;</code></p> <pre><code>using Temporalio.Workflows;\nusing XiansAi.Flow;\n\n[Workflow]\npublic class SimpleFlow: FlowBase\n{\n    [WorkflowRun]\n    public async Task&lt;string&gt; Run(string name)\n    {\n        var output = \"Hello\";\n        await DelayAsync(TimeSpan.FromSeconds(10));\n        output += \" World \" + name;\n        return await Task.FromResult(output + $\" {name}!\");\n    }\n}\n</code></pre> <p>Important</p> <p>The name workflow must be unique within your organization. You can see the list of workflow definitions on the XiansAI portal. You can change the name of the workflow by setting it at the <code>[Workflow]</code> attribute.</p> <pre><code>[Workflow(\"My New Named Flow\")]\npublic class SimpleFlow: FlowBase\n</code></pre> <p>Did you know?</p> <p>Did you know that Xians.ai is capable of long running (persistent) flows? This means you are able to delay a workflow for days or months as required. The workflow will be paused and resumed when the delay is over.</p>"},{"location":"getting-started/2-first-flow/#configuring-flow-visualization","title":"Configuring flow visualization","text":"<p>We need to configure bundling the source code of the flow file into the assembly. Although this is not needed to execute flows, this is required for the flow visualization to work. Do this by adding the following xml to your <code>.csproj</code> file:</p> <pre><code>  &lt;ItemGroup&gt;\n    &lt;!-- Embed the flow source files --&gt;\n    &lt;EmbeddedResource Include=\"SimpleFlow.cs\"&gt;\n        &lt;LogicalName&gt;%(Filename)%(Extension)&lt;/LogicalName&gt;\n    &lt;/EmbeddedResource&gt;\n  &lt;/ItemGroup&gt;\n</code></pre> <p>Basically this tells the compiler to include the <code>SimpleFlow.cs</code> file in the assembly as an embedded resource. </p> <p>Tip</p> <p>If the file is in a different folder, you need to update the <code>Include</code> attribute to the full path of the file. Example: <code>Include=\"MyNamespace/SimpleFlow.cs\"</code></p>"},{"location":"getting-started/2-first-flow/#registering-the-flow-runner","title":"Registering the Flow Runner","text":"<p>To register the flow, you need to add the new flow to Flow Runner on your <code>Program.cs</code> file. Update the <code>Program.cs</code> file with the following code:</p> <p><code>Program.cs &gt;</code></p> <pre><code>using XiansAi.Flow;\n\n// TODO: Get these values from the XiansAI portal\nvar config = ...\n\n// Create the flow runner\nvar flowRunner = new FlowRunnerService(config);\n\n// Define the flow\nvar flowInfo = new FlowInfo&lt;SimpleFlow&gt;();\n\n// Run the flow by passing the flow info\nTask simpleFlowTask = flowRunner.RunFlowAsync(flowInfo);\n\n// Wait for the flow to complete\nawait simpleFlowTask;\n</code></pre> <p>To run the flow, you can run the following command:</p> <pre><code>dotnet build    \ndotnet run\n</code></pre> <p>FlowRunner now waits to run the flows you are starting. You can start a new flow on the XiansAI portal by visiting the 'Flow Definitions' section and clicking on the 'Start New' button of your new flow Type (SimpleFlow).</p> <p>Name not unique error</p> <p>If you get the following error, you need to change the name of the flow. See above for how to change the name of the flow.</p> <pre><code>&gt; Bad Request: \"Another user has already used this flow type name SimpleFlow. Please choose a different flow name.\"\n</code></pre> <p></p> <p>You can see the flow definition details and the flow visualization on the XiansAI portal.</p> <p></p>"},{"location":"getting-started/2-first-flow/#run-the-flow","title":"Run the Flow","text":"<p>You can run the flow by clicking on the 'Start New' button on the flow definitions page. You should now see a new flow run on the 'Flow Runs' section of the XiansAI portal. Refresh if your run is not visible as it may require a few seconds to start.</p> <p></p>"},{"location":"getting-started/3-activiti-flow/","title":"Flow Activities","text":""},{"location":"getting-started/3-activiti-flow/#what-are-activities","title":"What are Activities?","text":"<p>Activities are the building blocks of a flow. They are the steps that are executed in a flow. Activities are defined as interfaces and implemented by classes.</p> <p>The <code>Flow</code> class is the main class that defines the flow. It contains the activities that are executed in the flow. <code>Flow</code> class is limited in it capabilities. It cannot perform any IO such as reading from a file or database. You can consider it as a <code>configuration file</code> of the flow, just that we have a full power of a programming language at our disposal.</p> <p>Flow Constraints</p> <p>Flow.ai uses the Temporal services to run flows. Temporal is an workflow engine system that allows you to run long running, robust and reliable flows. You can read more about Flow constraints here.</p> <p>You can create a new flow by creating a new class that inherits from <code>XiansAi.Flow.FlowBase</code>.</p> <p>Teaser Tip</p> <p>We are creating a simple flow. We will learn about more complex and meaningful flows that users <code>Agents</code> and <code>Instructions</code> in the next sections.</p> <p><code>SimpleFlow.cs &gt;</code></p> <pre><code>using Temporalio.Workflows;\nusing XiansAi.Flow;\n\n[Workflow]\npublic class SimpleFlow: FlowBase\n{\n    [WorkflowRun]\n    public async Task&lt;string&gt; Run(string name)\n    {\n        return $\"Hello {name}\";\n    }\n}\n</code></pre>"},{"location":"getting-started/3-activiti-flow/#registering-the-flow","title":"Registering the Flow","text":"<p>To register the flow, you need to add the new flow to Flow Runner on your <code>Program.cs</code> file. Update the <code>Program.cs</code> file with the following code:</p> <p><code>Program.cs &gt;</code></p> <pre><code>using XiansAi.Flow;\n\n// TODO: Get these values from the XiansAI portal\nvar config = ...\n\n// Runner for the flow\nvar flowRunner = new FlowRunnerService(config);\n\n// Define the flow\nvar flowInfo = new FlowInfo&lt;SimpleFlow&gt;();\n\n// Add activities to the flow\nflowInfo.AddActivity&lt;IActivityOne&gt;(new ActivityOne());\nflowInfo.AddActivity&lt;IActivityTwo&gt;(new ActivityTwo());\n\n// Run the flow\nTask simpleFlowTask = flowRunner.RunFlowAsync(flowInfo, CancellationToken.None);\n\n// Wait for the flow to complete\nawait simpleFlowTask;\n</code></pre>"},{"location":"getting-started/3-activiti-flow/#configuring-flow-visualization","title":"Configuring flow visualization","text":"<p>You need to configure bundling the source code of the flow file into the assembly. This is done by adding the following to your <code>csproj</code> file:</p> <pre><code>  &lt;ItemGroup&gt;\n    &lt;!-- Embed the flow source files --&gt;\n    &lt;EmbeddedResource Include=\"SimpleFlow.cs\"&gt;\n        &lt;!-- Keep the resource name simple since files are in root --&gt;\n        &lt;LogicalName&gt;%(Filename)%(Extension)&lt;/LogicalName&gt;\n    &lt;/EmbeddedResource&gt;\n  &lt;/ItemGroup&gt;\n</code></pre> <p>It is recommended to keep the flow files on the root of the project.</p>"},{"location":"getting-started/3-activiti-flow/#running-the-flow","title":"Running the Flow","text":"<p>To run the flow, you can run the following command:</p> <pre><code>dotnet build    \ndotnet run\n</code></pre> <p>Program now waits to run the flows you are starting. You can start a new flow on the XiansAI portal by visiting the 'Flow Definitions' section and clicking on the 'Start New' button of your new flow Type (SimpleFlow).</p> <p></p>"},{"location":"getting-started/3-activiti-flow/#monitoring-the-flow","title":"Monitoring the Flow","text":"<p>You can monitor the flow by visiting the 'Flow Runs' section of the XiansAI portal.</p> <p></p> <p>You can see the flow run details by clicking on the flow run id. For activities, you are able to see details such as:</p> <ul> <li>Activity Inputs</li> <li>Activity Outputs</li> <li>Activity Start Time</li> <li>Activity End Time</li> <li>Activity Instructions</li> <li>Activity Agents</li> </ul> <p></p>"}]}